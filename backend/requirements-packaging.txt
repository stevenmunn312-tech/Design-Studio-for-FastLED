# Desktop-distribution build dependencies. The launcher bundle also needs the
# production helper dependencies because PyInstaller freezes that environment.
-r requirements.txt
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
