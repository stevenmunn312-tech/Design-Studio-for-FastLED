import type { CSSProperties } from 'react'

/** Shared geometry for every patch-bay socket, including custom node bodies. */
export const NODE_HANDLE_STYLE = {
  width: 12,
  height: 12,
  borderRadius: '50%',
  border: 'none',
} satisfies CSSProperties
